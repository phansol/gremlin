cran = "http://cran.r-project.org"
if (!suppressWarnings(require("tidyverse"))){install.packages("tidyverse", repos = cran)}
if (!suppressWarnings(require("gsubfn"))){install.packages("gsubfn", repos = cran)}
if (!suppressWarnings(require("dummies"))){install.packages("dummies", repos = cran)}
if (!suppressWarnings(require("data.table"))){install.packages("data.table", repos = cran)}
if (!suppressWarnings(require("gbm"))){install.packages("gbm", repos = cran)}
